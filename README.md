# ProductAssignment
Product Admin Dashboard built with HTML, CSS and vanilla JavaScript. It uses the DummyJSON API through Axios for login, product CRUD, debounced search, category filter, sorting, pagination and URL-based state.
